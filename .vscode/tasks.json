{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run Java DSA",
      "type": "shell",
      "command": "bash",
      "args": [
        "-c",
        "# Compile all Java files in functions_arrays and run the chosen main class\njavac functions_arrays/*.java && java -cp \"${workspaceFolder}\" functions_arrays.anyBaseSubtraction < functions_arrays/input.txt > functions_arrays/output.txt"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "presentation": {
        "echo": true,
        "reveal": "always",
        "panel": "shared"
      },
      "problemMatcher": "$javac",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}